<header class="Header">
	<div class="guts">
		<h1>What is the <a href="/">lifespan of a site</a>?</h1>
	</div>
</header>

<style>
	header {
		background-color: var(--color-secondary);
		box-sizing: border-box;
		color: var(--color-primary);
		position: fixed;
		width: 100vw;
		z-index: 100;
	}
	.guts {
		box-sizing: border-box;
		padding: calc(var(--unit) * 0.5) var(--unit);
		width: 100%;
	}
	h1 {
		font-size: var(--unit);
		font-style: italic;
		font-weight: 100;
		line-height: 1;
	}
	h1 a {
		color: inherit;
		font-weight: 900;
	}
</style>